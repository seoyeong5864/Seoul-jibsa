// Front/src/components/notices/NoticeFilterBar.tsx
type Props<T> = {
  value: T;
  onChange: (value: T) => void;
};

export default function NoticeFilterBar<T,>({ value, onChange }: Props<T>) {
  void value;
  void onChange;
  return null;
}
